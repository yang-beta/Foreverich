# 2026-08-16｜品牌故事文字間距、地平線、洸語牆最舊留言 V5

本次只需要替換 3 個網站檔案：

- `css/style.css`
- `js/canvas.js`
- `js/main.js`

不需要修改：
- `index.html`
- `css/mobile-fixes.css`
- `js/animations.js`

---

## 1. 品牌故事第一行 / 第三行間距

原本第一、第三行每個分段都是獨立 flex item，且設定：

```css
gap: .72em;
```

加上每個 segment 自己又有 `letter-spacing`，在分段邊界會產生「gap + 尾端字距」雙重空間。

因此視覺上會看到：

```text
水光；     「 限 」
希望你，     不僅是...
情感，       更讓...
```

本次改為：

```css
gap: .08em;
```

並抵消非最後一段尾端多出來的 letter-spacing。

動畫仍然是分段先後出現，
但尚未出現的 segment 仍保留版面寬度，
所以文字位置不會因動畫重新排列。

---

## 2. 地平線透明度

透明度不再用「第幾條線」直接估算，
而是根據每一條線真正的 Y 座標計算。

規則：

- 最上方第一條 = 25%
- 第二條開始逐步增加
- 到人物腳底位置 = 65%
- 人物腳底以下 = 維持 65%

所以即使地平線的線距是非線性的，
視覺透明度仍會真正由上而下平順增加。

---

## 3. 隨機靜態品牌色橫線

品牌故事每次進頁時：

- 隨機選 4～7 條水平線
- 使用品牌主色 `#FFB088`
- 位置在該次頁面停留期間完全不動
- 不會每 frame 重新 random，因此不閃爍

原本 P1 式「由上往下移動的品牌色掃光」仍保留。

也就是現在同時有：

```text
中性水平線
＋
幾條固定珊瑚杏線
＋
由上往下移動的珊瑚杏掃光
```

---

## 4. 洸語牆左右卡片改成資料庫真實留言

中央留言：

```js
.order('created_at', { ascending:false })
.limit(12)
```

仍然顯示最新 12 筆。

左右延伸留言另外查：

```js
.order('created_at', { ascending:true })
.limit(6)
```

也就是資料庫最舊的 6 筆。

排列：

```text
左側：最舊第 1～3 筆
中央：最新 12 筆
右側：最舊第 4～6 筆
```

左右卡片使用和正常留言相同的：

- 對象
- 日期
- 留言正文
- 場景 / 情感 metadata
- 對象背景 theme

但不會設定：

- `tabIndex`
- `role="button"`
- click event
- keydown event

所以只能觀看，不能點擊開卡。

如果「最舊 6 筆」讀取失敗，
中央最新 12 筆仍會正常顯示，不會讓整面洸語牆一起報錯。

---

## 檢查

- `canvas.js` Node syntax PASS
- `main.js` Node syntax PASS
