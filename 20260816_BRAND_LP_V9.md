# 2026-08-16｜LP 字圖放大 + 品牌故事 Slogan 括號 V9

本次需要替換 4 個網站檔案：

- `index.html`
- `css/style.css`
- `css/mobile-fixes.css`
- `js/animations.js`

---

## 1. LP「洸／限」尺寸再放大 30%

以 V8 的實際尺寸為基準直接乘以 1.3。

### 桌機

V8：

```css
clamp(15.6px, 1.6vw, 24.4px)
```

V9：

```css
clamp(20.28px, 2.08vw, 31.72px)
```

計算：

- 15.6 × 1.3 = 20.28
- 1.6 × 1.3 = 2.08
- 24.4 × 1.3 = 31.72

### 手機 / 平板

V8：

```css
clamp(10.8px, 3.2vw, 15.6px)
```

V9：

```css
clamp(14.04px, 4.16vw, 20.28px)
```

### 距離

這次完全不改：

```css
桌機：-235px
手機：-65px
```

因此只有字圖放大，不會再次改變與 Logo 的距離。

---

## 2. 最後兩句 Slogan 左右加入 [ ]

HTML 新增左右兩個獨立 bracket element：

```html
brand-story-final-bracket-left
brand-story-final-bracket-right
```

括號使用 CSS border 畫成細線方括號，
包住：

```text
因為有限　所以珍惜
因為記得　所以重逢
```

整個兩行文字群組。

不是每行各一組括號。

---

## 3. 括號出現時間

動畫順序：

```text
因為有限　所以珍惜
↓ 1.5 秒
因為記得　所以重逢
↓ 第二句完整出現
等待 1.0 秒
↓
左右 [ ] 同時淡入＋由中央向上下展開
```

括號動畫：

```js
autoAlpha: 0 → 1
scaleY: .45 → 1
duration: .62
```

如果按 Skip，
則會直接顯示：

- 最後兩句
- 左右括號
- 人物

---

## 4. 手機 / 平板

括號位置另外放在 `mobile-fixes.css`，
避免桌機尺寸直接套到手機造成超出畫面。

`style.css` 仍只負責正式桌機基礎樣式，
mobile RWD 繼續留在 `mobile-fixes.css`。

---

## 檢查

- `animations.js` Node syntax PASS
