# 2026-08-16｜品牌故事黑畫面、P4 地形、P1 人物修正

本次只需要修改 2 個網站檔案：

- `index.html`
- `js/canvas.js`

另外附本說明檔，不需要修改：
- `style.css`
- `mobile-fixes.css`
- `main.js`
- `animations.js`

---

## 1. 品牌故事整頁黑畫面

### 原因

品牌故事 HTML 被放在：

```html
</div><!-- #sectionsWrapper 已結束 -->

<div id="brandStorySection">
```

也就是 `brandStorySection` 實際位於 `sectionsWrapper` 外面。

但 `main.js` 使用：

```js
const pageElements = gsap.utils.toArray('.section-page');
```

所以仍會把品牌故事算成下一頁，再把：

```js
sectionsWrapper.style.transform =
  `translateY(-${currentPageIndex * 100}vh)`;
```

移動到品牌故事頁的位置。

結果是：

- wrapper 被移到第 9 頁的位置；
- brandStorySection 卻根本不在 wrapper 裡；
- viewport 最後只看到 body / section 的黑色背景。

因此 Canvas、文字、人物、飛鳥會「一起消失」。

### 修正

現在 DOM 改為：

```html
<div id="sectionsWrapper">
  ...
  <div id="memoryWallSection">...</div>

  <div id="brandStorySection">...</div>
</div>
```

品牌故事已成為 `sectionsWrapper` 的直屬 `.section-page`。

這次不需要改 `main.js / animations.js / style.css`。

---

## 2. P4 隨機地形

原本 2～4 個隨機局部 feature 會隨機產生：

- amplitude < 0：向上凸
- amplitude > 0：向下凹

因為最後座標是：

```js
y = baseY + deformation;
```

本次改為所有隨機 feature：

```js
amplitude < 0
```

因此只會形成「凸出地面的小山丘」，不再形成局部坑洞。

仍維持：

- 每次進入 P4 重新隨機
- 2 個主要山丘
- 再加 0～2 個小山丘
- 總數 2～4 個
- 局部倍率維持 1.80

P4 原本的大尺度坡形、底層波浪、掃光全部不變。

---

## 3. P1 人物透明度

修改：

```js
const PERSON_OPACITY = 0.65;
```

為：

```js
const PERSON_OPACITY = 0.40;
```

也就是 P1 人物改為 40% 不透明度。

人物：
- 大小不變
- Y 位置不變
- 陰影不變
- 動畫時間不變

---

## 驗證

已確認：

- `brandStorySection` 是 `sectionsWrapper` 的直屬 section-page
- `canvas.js` 通過 Node syntax check
- 沒有修改其他頁面 CSS
- 沒有修改全站 navigation lifecycle
